<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RunningCrew" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryVariant">@color/main_color_variant</item>
        <item name="colorOnPrimary">@color/text_color</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/nav_bar_top_line</item>
        <!-- Customize your theme here. -->

    </style>
    <style name="numberPickerCustomSize">
        <item name="android:textSize">80sp</item>
        <item name="android:editTextColor">@color/black</item>
    </style>
</resources>